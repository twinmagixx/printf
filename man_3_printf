.\" Manpage for gioandlina.
.\" Contact mozamanebaloyi1994@gmail.com or gio in to correct errors or typos.
.TH man 3 "15 March 2022" "1.0" "_printf man page"
.SH NAME
.B _printf
- Formatted output conversion
.SH SYNOPSIS
.B #include "main.h"

.B int _printf(const char *format, ...);

.B int opFunction(int cont, va_list list, typedate tipos[], const char *format);
.SH DESCRIPTION
The printf() functions produce the output according to the format we need.

.B
Return value

Upon successful return, these functions return the number of characters printed (excluding the null byte used to end output to strings).

.B 
conversion specifiers:

c:
The function prints a character, it can be entered in ASCCI format in the range of> = 32 and 127 <= which are the printable ranges within the function.

s:
Receive as an argument a char pointer or array string that does not include the null termination, in case a NULL is sent it will print (null).

%:
If the next sign is equal to the percent sign then% will be printed in that case.

d and i:
Convert the parameter number to a base number 10, return 0 if the argument is empty.

.SH OPTIONS
others
.SH SEE ALSO
printf(3)
.SH BUGS
No known bugs.
